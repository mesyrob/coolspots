apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coolspots-data
  namespace: coolspots
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
